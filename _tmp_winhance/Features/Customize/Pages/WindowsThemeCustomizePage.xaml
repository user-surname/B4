<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Winhance.UI.Features.Customize.Pages.WindowsThemeCustomizePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Winhance.UI.Features.Customize.Pages"
    xmlns:commonControls="using:Winhance.UI.Features.Common.Controls"
    x:DefaultBindMode="OneWay"
    NavigationCacheMode="Disabled">

    <Page.Resources>
        <!-- CollectionViewSource for grouped settings -->
        <CollectionViewSource
            x:Name="GroupedSettingsSource"
            Source="{x:Bind ViewModel.WindowsThemeViewModel.GroupedSettings}"
            IsSourceGrouped="True"/>
    </Page.Resources>

    <commonControls:SettingsListView
        GroupedSettingsSource="{x:Bind GroupedSettingsSource.View, Mode=OneWay}"
        IsLoading="{x:Bind ViewModel.IsLoading}"
        HasNoSearchResults="{x:Bind ViewModel.HasNoSearchResults}"/>
</Page>
